---
tags: [pmie, session, f4]
fecha: 2026-07-31
proyecto: pmie
estado: F4 COMPLETA
commit: e44d54a
---

# Sesión 31 Jul 2026 — PMIE Fase 4: Decision Engine

## Qué se hizo
1. **packages/decision** completo: engine.ts (recommendMethod/recommendMarket/stabilize), merchant.ts (buildProfiles/detectStrategy), types.ts, index.ts. 13 tests.
   - Bug de buildProfiles corregido: top1 se calcula por MÉTODO global, no por grupo de merchant.
2. **DB**: tablas alerts, decision_state, merchant_profiles (migración 0004). Columna de método = `pay_method` en DB.
3. **Worker** `decision.ts`: buildSignals (agregados 1h + spread cruzando lado opuesto + persistencia), job cada 5min, 5 tipos de alerta, refresh perfiles 15min.
4. **API**: endpoints `/api/decisions/{state,history,alerts,profiles}` + `POST /internal/alerts` (token) + Socket.IO `pmie:alert`.
5. **Dashboard**: vista Decisiones (cards de estado, alertas con socket en vivo + flash, tabla de merchants).

## Bugs encontrados y fixeados
- `column bucket_time does not exist` en buildSignals → el CTE `bounds` no tenía `FROM ad_aggregates_1h` final (el fallo solo aparecía vía pool del worker, no en psql).
- Columnas DB se llaman `pay_method` (no `method`) → las queries SQL crudas de la API referenciaban `method`.

## Verificación
- 32/32 tests (decision 13, scanner 8, diff 7, api 4). Typecheck monorepo OK. `vite build` OK.
- Socket.IO verificado end-to-end: POST /internal/alerts → cliente recibe `pmie:alert`.
- Decisiones reales coherentes: PagoMovil VES/SELL pausa por circuit_breaker (sin oferta real), Banesco VES/BUY baja siguiendo top.

## Próxima fase (F5 — Prediction Engine)
- Distribución por hora (mejor/peor horario comprar y vender)
- Medias móviles + percentiles por método/monto
- Probabilidad de venta en próxima hora (rotación histórica)
- Spread esperado por hora/día
- Señal VES de macro-riesgo (expansión monetaria, gap tasa oficial)
- Backtest modelo vs realidad
- DONE when: predicciones horario con backtest ≥ 60% acierto + dashboard "mejor horario hoy"

## Handoff
Servicios vivos: worker 3007, API 3008 (con socket), dash 5174. Reiniciar con fuser -k puertos + nohup. Logs en /tmp/opencode/.
